import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['i.ibb.co', 'pbs.twimg.com'], // Add allowed image domains here
  },
};

export default nextConfig;
